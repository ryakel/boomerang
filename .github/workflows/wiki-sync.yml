name: Sync Wiki

on:
  push:
    branches: [main]
    paths: ['wiki/**']

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Checkout wiki
        uses: actions/checkout@v4
        with:
          repository: ${{ github.repository }}.wiki
          path: .wiki
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Sync wiki content
        run: |
          # Mirror wiki/*.md into the wiki repo, removing pages that no longer
          # exist in the source (a plain `cp` never deletes stale pages, so a
          # file removed from wiki/ would linger on the published wiki forever).
          rsync -a --delete --exclude='.git' wiki/ .wiki/
          cd .wiki
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "No wiki changes"
          else
            git commit -m "Sync wiki from repo wiki/ directory"
            git push
          fi
